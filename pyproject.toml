[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "predicting_flight_arrival_delays"
version = "0.0.1"
description = "Predicting Arrival Delays in Commercial Flights"
authors = [
  { name = "Alessandro Bevilacqua" },
]
license = { file = "LICENSE" }
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License"
]
dependencies = [
    "dagshub>=0.7.1",
    "dvc>=3.67.1",
    "dvc-s3>=3.3.0",
    "holidays>=0.101",
    "imbalanced-learn>=0.14.2",
    "ipython",
    "jupyterlab",
    "lightgbm>=4.7.0",
    "loguru",
    "matplotlib",
    "mlflow>=3.15.1",
    "notebook",
    "numpy",
    "pandas",
    "pip",
    "playwright>=1.62.0",
    "pyarrow>=25.0.0",
    "pytest",
    "python-dotenv",
    "ruff",
    "scikit-learn",
    "seaborn>=0.13.2",
    "timezonefinder>=8.2.5",
    "tqdm",
    "typer",
]
requires-python = "~=3.12.0"


[tool.ruff]
line-length = 99
src = ["predicting_flight_arrival_delays"]
include = ["pyproject.toml", "predicting_flight_arrival_delays/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["predicting_flight_arrival_delays"]
force-sort-within-sections = true

